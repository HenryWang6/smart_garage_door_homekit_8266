name: Release

on:
  release:
     types: [published]

permissions:
  contents: write
  actions: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
            ref: main
            submodules: true

      - name: Tag
        id: tag
        uses: JinoArch/get-latest-tag@latest

      - name: Update version on manifest.json
        uses: amochkin/action-json@v1
        id: write_version
        with:
          mode: write
          file: docs/manifest.json
          property: version
          value: ${{ steps.tag.outputs.latestTag }}
          value_type: string
          
      - name: Output read value of 'version' property
        run: echo ${{ steps.write_version.outputs.value }}
        shell: bash

      - name: Update firmware/*.bin path on manifest.json
        uses: amochkin/action-json@v1
        id: builds_parts_path
        with:
          mode: write
          file: docs/manifest.json
          property: builds.0.parts.0.path
          value: https://github.com/${{ github.repository }}/releases/download/${{ steps.tag.outputs.latestTag }}/HTGDO-${{ steps.tag.outputs.latestTag }}.bin
          value_type: string
          
      - name: Commit updated manifest.json to repo
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update manifest.json for release ${{ steps.tag.outputs.latestTag }}"
          file_pattern: docs/manifest.json

      - name: Attach manifest.json to Release
        uses: AButler/upload-release-assets@v3.0
        with:
          files: "docs/manifest.json"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          release-tag: ${{ steps.tag.outputs.latestTag }}

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio
          
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install PlatformIO Core
        run: |
            python -m pip install --upgrade pip
            pip install --upgrade platformio

      - name: Build PlatformIO Project
        run: pio run -e ratgdo_esp8266_hV25

      - name : md5sum Firmware.bin
        run: |
          cd .pio/build/ratgdo_esp8266_hV25
          md5sum firmware.bin | awk '{print $1}' > firmware.md5

      - name: Rename Firmware Files
        run: |
          mv .pio/build/ratgdo_esp8266_hV25/firmware.bin .pio/build/ratgdo_esp8266_hV25/HTGDO-${{ steps.tag.outputs.latestTag }}.bin
          mv .pio/build/ratgdo_esp8266_hV25/firmware.elf .pio/build/ratgdo_esp8266_hV25/HTGDO-${{ steps.tag.outputs.latestTag }}.elf
          mv .pio/build/ratgdo_esp8266_hV25/firmware.md5 .pio/build/ratgdo_esp8266_hV25/HTGDO-${{ steps.tag.outputs.latestTag }}.md5

      - name: Attach Bundle - Firmware.bin
        uses: AButler/upload-release-assets@v3.0
        with:
          files: ".pio/build/ratgdo_esp8266_hV25/*.bin"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          release-tag: ${{ steps.tag.outputs.latestTag }}
          
      - name: Attach Bundle - Firmware.md5
        uses: AButler/upload-release-assets@v3.0
        with:
          files: ".pio/build/ratgdo_esp8266_hV25/*.md5"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          release-tag: ${{ steps.tag.outputs.latestTag }}
          
      - name: Attach Bundle - Firmware.elf
        uses: AButler/upload-release-assets@v3.0
        with:
          files: ".pio/build/ratgdo_esp8266_hV25/*.elf"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          release-tag: ${{ steps.tag.outputs.latestTag }}

      # - name: Sleep for 2 minutes before publishing to Discord
      #   run: sleep 120s
      #   shell: bash
      #
      # - name: Latest Release
      #   if: ${{ github.event.release.prerelease == false }}
      #   uses: SethCohen/github-releases-to-discord@v1.13.1
      #   with:
      #     webhook_url: ${{ secrets.DISCORD_WEBHOOK_URL }}
      #     color: "5723991"
      #     username: "HTGDO"
      #     avatar_url: "https://avatars.githubusercontent.com/u/144837877?s=200&v=4"
      #     footer_title: "HTGDO Release"
      #     footer_icon_url: "https://avatars.githubusercontent.com/u/144837877?s=200&v=4"
      #     footer_timestamp: true
      #     
      # - name: Pre-Release
      #   if: ${{ github.event.release.prerelease == true }}
      #   uses: SethCohen/github-releases-to-discord@v1.13.1
      #   with:
      #     webhook_url: ${{ secrets.DISCORD_WEBHOOK_URL }}
      #     color: "5723991"
      #     username: "HTGDO"
      #     avatar_url: "https://avatars.githubusercontent.com/u/144837877?s=200&v=4"
      #     footer_title: "Pre-Release: HTGDO"
      #     footer_icon_url: "https://avatars.githubusercontent.com/u/144837877?s=200&v=4"
      #     footer_timestamp: true
